name: Daily Horizon Summary

# Reliability note: GitHub Actions' built-in `on: schedule` is unreliable.
# Empirically a single `0 0 * * *` cron can fire 18h late, twice in one
# day, or be skipped entirely. The fix is to fire every 5 minutes and use
# a time-window + state-dedup gate at the start of the job:
#   - Two valid windows (UTC):
#       23:00–23:55 (= 07:00–07:55 Beijing): primary, lands the post
#         before the VPS sync at 08:17 BJT
#       00:00–01:00 (= 08:00–09:00 Beijing): legacy safety net so a
#         missed early run still gets covered
#     Outside both windows the job exits immediately. GHA still wakes
#     the runner but the run is a few seconds of bash, well within
#     free-plan minutes (~150 min/month). 60 minutes = 12 */5 triggers,
#     statistically near-impossible to miss, and the state-dedup still
#     keeps it down to a single run/day.
#   - Inside either window, if today's
#     `docs/_posts/YYYY-MM-DD-horizon-zh.md` already exists on master,
#     the job exits. This handles the case where multiple */5 triggers
#     across both windows all wake up before any single run finishes
#     deploying.
# The first run that survives both checks is the one that actually
# generates the post.
#
# Local mavis-cron backstop (~/.mavis/cron/today-task-daily-summary-backstop)
# fires workflow_dispatch at 08:30 BJT if today's post is missing — final
# backstop if GHA misses both windows.

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

on:
  # 2026-08-08: ALL scheduled triggers DISABLED per owner directive.
  # GitHub repo is now backup-only — no automated runs. The two */5 crons
  # fired up to 288 times/day and every failure sent an email, which was
  # the source of the daily error-mail flood. workflow_dispatch is kept
  # so a run can still be triggered manually (gh workflow run ...).
  # To re-enable automation, revert this commit (restores the 5 crons below).
  #
  # schedule:
  #   - cron: '*/5 * * * *'  # daily window gate (23:00-01:00 UTC = 07:00-09:00 BJT)
  #   - cron: '0 10 * * 1'   # weekly rollup, Monday 10:00 Asia/Shanghai
  #   - cron: '0 10 8 * *'   # monthly rollup, 8th 10:00 Asia/Shanghai
  #   - cron: '0 10 15 7 *'  # yearly H1, 15 July 10:00 Asia/Shanghai
  #   - cron: '0 10 15 1 *'  # yearly H2, 15 January 10:00 Asia/Shanghai
  workflow_dispatch:
    inputs:
      force:
        description: 'Bypass state dedup and regenerate today even if the post already exists on master.'
        required: false
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'true'

jobs:
  daily-summary:
    runs-on: ubuntu-latest
    # Serialize per-day so two */5 triggers in the same window can't both
    # run LLM generation + push the same post.
    concurrency:
      group: daily-summary-${{ inputs.date || github.event.head_commit.timestamp || 'live' }}
      cancel-in-progress: false
    permissions:
      contents: write

    steps:
      # Checkout must come BEFORE the gate, because the gate uses
      # `git show origin/master:docs/...` which needs a git repo. Doing
      # the gate first means "fatal: not a git repository" — that's
      # exactly what bit run #49 on 2026-06-22.
      - name: Checkout main
        uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0

      - name: Window + state dedup (gate via output, NOT exit 0)
        id: gate
        run: |
          # Cron */5 fires 288x/day. Filter down to the daily-generation
          # windows: 23:00–23:55 UTC (= 07:00–07:55 BJT, primary — must
          # land before VPS sync at 08:17 BJT) or 00:00–01:00 UTC
          # (= 08:00–09:00 BJT, legacy safety net). Inside either window
          # skip if today's post is already on master — that means a
          # previous */5 trigger in either window already finished.
          #
          # CRITICAL: do NOT use `exit 0` to "skip" — exit 0 only exits
          # the current step, the rest of the job keeps running. Use
          # $GITHUB_OUTPUT `should_run` + `if: steps.gate.outputs.should_run == 'true'`
          # on every later step instead.
          UTC_NOW=$(date -u +'%H%M')
          BJT_NOW=$(TZ=Asia/Shanghai date +'%H%M')
          echo "UTC_NOW=$UTC_NOW BJT_NOW=$BJT_NOW, trigger=${{ github.event_name }}"
          # Manual triggers (workflow_dispatch) bypass the time window —
          # operator wants to run NOW, even outside the scheduled windows.
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "workflow_dispatch — bypassing time-window gate."
          else
            # Primary window: 23:00–23:55 UTC (= 07:00–07:55 BJT)
            if [ "$UTC_NOW" -ge "2300" ] && [ "$UTC_NOW" -lt "2400" ]; then
              :
            # Legacy safety-net window: 00:00–01:00 UTC (= 08:00–09:00 BJT)
            elif [ "$UTC_NOW" -ge "0000" ] && [ "$UTC_NOW" -lt "0200" ]; then
              :
            else
              echo "Outside 23:00–01:00 UTC generation window (07:00–09:00 BJT), skipping."
              echo "should_run=false" >> "$GITHUB_OUTPUT"
              exit 0
            fi
          fi
          DATE=$(TZ=Asia/Shanghai date +'%Y-%m-%d')
          POST="docs/_posts/${DATE}-horizon-zh.md"
          echo "DATE=$DATE POST=$POST"
          git fetch origin master --depth=1
          if git show "origin/master:${POST}" > /dev/null 2>&1; then
            if [ "${{ inputs.force }}" = "true" ]; then
              echo "force=true — today's post exists on master, but regenerating anyway (operator override)."
            else
              echo "Today's post already on master, skipping."
              git show "origin/master:${POST}" | head -3
              echo "should_run=false" >> "$GITHUB_OUTPUT"
              exit 0
            fi
          fi
          echo "should_run=true" >> "$GITHUB_OUTPUT"
          echo "In window and no existing post — proceeding with daily run."

      - name: Set up Python
        if: steps.gate.outputs.should_run == 'true'
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install uv
        if: steps.gate.outputs.should_run == 'true'
        uses: astral-sh/setup-uv@v6

      - name: Install dependencies
        if: steps.gate.outputs.should_run == 'true'
        run: uv sync

      - name: Prepare GitHub Actions config
        if: steps.gate.outputs.should_run == 'true'
        run: cp data/config.github.json data/config.json

      - name: Run Horizon
        id: primary_run
        if: steps.gate.outputs.should_run == 'true'
        continue-on-error: true
        env:
          MINIMAX_API_KEY: ${{ secrets.MINIMAX_API_KEY }}
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: uv run horizon --hours 24

      - name: Run Horizon (EE edition)
        if: steps.gate.outputs.should_run == 'true'
        continue-on-error: true
        env:
          MINIMAX_API_KEY: ${{ secrets.MINIMAX_API_KEY }}
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: |
          cp data/config.ee.json data/config.json
          # Pin date to Beijing time — GHA's primary window UTC 23:00–23:55
          # (= 07:00–07:50 BJT) crosses the UTC date boundary, so a
          # `date -u`-derived filename drifts and merge_boards.py silently
          # sees only 1/4 boards.
          DATE=$(TZ=Asia/Shanghai date +'%Y-%m-%d')
          cp "data/summaries/${DATE}-horizon-zh.md" "/tmp/${DATE}-horizon-zh.md.bak" || true
          set +e
          uv run horizon --hours 24
          RUN_RC=$?
          set -e
          if [ -f "data/summaries/${DATE}-horizon-zh.md" ]; then
            mv "data/summaries/${DATE}-horizon-zh.md" "data/summaries/${DATE}-ee-zh.md"
          fi
          exit "$RUN_RC"

      - name: Run Horizon (global news edition)
        if: steps.gate.outputs.should_run == 'true'
        continue-on-error: true
        env:
          MINIMAX_API_KEY: ${{ secrets.MINIMAX_API_KEY }}
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: |
          cp data/config.global.json data/config.json
          DATE=$(TZ=Asia/Shanghai date +'%Y-%m-%d')
          set +e
          uv run horizon --hours 24
          RUN_RC=$?
          set -e
          if [ -f "data/summaries/${DATE}-horizon-zh.md" ]; then
            mv "data/summaries/${DATE}-horizon-zh.md" "data/summaries/${DATE}-global-zh.md"
          fi
          exit "$RUN_RC"

      - name: Run Horizon (github-trending edition)
        if: steps.gate.outputs.should_run == 'true'
        continue-on-error: true
        env:
          MINIMAX_API_KEY: ${{ secrets.MINIMAX_API_KEY }}
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: |
          cp data/config.github-trending.json data/config.json
          DATE=$(TZ=Asia/Shanghai date +'%Y-%m-%d')
          set +e
          uv run horizon --hours 24
          RUN_RC=$?
          set -e
          if [ -f "data/summaries/${DATE}-horizon-zh.md" ]; then
            mv "data/summaries/${DATE}-horizon-zh.md" "data/summaries/${DATE}-github-trending-zh.md"
          fi
          cp "/tmp/${DATE}-horizon-zh.md.bak" "data/summaries/${DATE}-horizon-zh.md" || true
          exit "$RUN_RC"

      - name: Append EE daily knowledge card
        if: steps.gate.outputs.should_run == 'true'
        continue-on-error: true
        env:
          MINIMAX_API_KEY: ${{ secrets.MINIMAX_API_KEY }}
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: |
          DATE=$(TZ=Asia/Shanghai date +'%Y-%m-%d')
          uv run python scripts/append_knowledge.py --edition ee --date "$DATE"

      - name: Merge four boards into one daily page
        if: steps.gate.outputs.should_run == 'true'
        continue-on-error: true
        run: |
          uv run python scripts/merge_boards.py --date $(TZ=Asia/Shanghai date +'%Y-%m-%d')

      - name: Deploy docs to master for Pages
        if: steps.gate.outputs.should_run == 'true'
        run: |
          DATE=$(TZ=Asia/Shanghai date +'%Y-%m-%d')
          test -s "data/summaries/${DATE}-horizon-zh.md"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          # -f: force discard local changes when switching branches
          # (otherwise git refuses if main and master both have today's
          # same-dated _posts file).
          git checkout -f master
          git pull origin master || true
          # Refresh static site assets (config, css, includes) from main
          # but DO NOT rm -rf docs/ — that wipes out historical
          # _posts/ files accumulated by previous daily runs.
          git checkout -f main -- docs/_config.yml docs/_includes docs/assets .github/scripts 2>/dev/null || true
          mkdir -p docs/_posts
          # Bring forward any _posts that already exist on master (from
          # earlier runs) and overlay today's freshly-written summary
          # on top. ``cp -n`` would skip writes — we want to overwrite
          # today's file, so use plain cp.
          git checkout -f master -- docs/_posts 2>/dev/null || true
          cp data/summaries/*.md docs/_posts/ || true
          git add -f docs/
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            DATE=$(date +'%Y-%m-%d')
            git commit -m "📝 Daily Summary: $DATE"
            git push origin master
          fi

      - name: Mark primary scoring/generation failure after publishing incident page
        if: steps.gate.outputs.should_run == 'true' && steps.primary_run.outcome == 'failure'
        run: |
          echo "::error::Primary Horizon run failed. Its persisted incident report was published to master for downstream alerting."
          exit 1

  weekly-summary:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - uses: astral-sh/setup-uv@v6
      - run: uv sync
      - run: cp data/config.github.json data/config.json
      - name: Fetch prior reports from master → data/summaries/
        run: |
          # Chain-mode rollups (weekly/monthly/yearly) read prior reports from
          # data/summaries/, but that dir is .gitignored and empty on a fresh
          # GHA runner. Materialise master docs/_posts/ into data/summaries/
          # so list_recent_summaries() has input.
          #
          # Files land with the same -horizon-zh.md suffix the storage layer
          # already keys on, so prior daily/weekly/monthly reports all
          # flow through the same filename matcher.
          git fetch origin master --depth=1
          mkdir -p data/summaries
          # NUL-delimited listing: plain ls-tree quotes non-ASCII paths
          # (core.quotePath) and word-splitting breaks on spaces.
          git ls-tree -r --name-only -z origin/master docs/_posts/ | while IFS= read -r -d '' f; do
            base=$(basename "$f")
            git show "origin/master:$f" > "data/summaries/$base"
          done
          echo "Loaded $(ls data/summaries/ | wc -l) prior report(s) from master:"
          ls data/summaries/ | tail -10
      - name: Run weekly roll-up
        env:
          MINIMAX_API_KEY: ${{ secrets.MINIMAX_API_KEY }}
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: uv run horizon weekly
      - name: Deploy weekly to Pages
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git checkout -f master
          git pull origin master || true
          git checkout -f main -- docs/_config.yml docs/_includes docs/assets .github/scripts 2>/dev/null || true
          mkdir -p docs/_posts
          git checkout -f master -- docs/_posts 2>/dev/null || true
          cp data/summaries/*.md docs/_posts/ || true
          git add -f docs/
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "📅 Weekly Summary"
            git push origin master
          fi

  monthly-summary:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - uses: astral-sh/setup-uv@v6
      - run: uv sync
      - run: cp data/config.github.json data/config.json
      - name: Fetch prior reports from master → data/summaries/
        run: |
          # See weekly-summary job above for the full rationale. Chain-mode
          # rollups read from data/summaries/ which is .gitignored; we
          # materialise master docs/_posts/ into it on every rollup job.
          git fetch origin master --depth=1
          mkdir -p data/summaries
          # NUL-delimited listing: plain ls-tree quotes non-ASCII paths
          # (core.quotePath) and word-splitting breaks on spaces.
          git ls-tree -r --name-only -z origin/master docs/_posts/ | while IFS= read -r -d '' f; do
            base=$(basename "$f")
            git show "origin/master:$f" > "data/summaries/$base"
          done
          echo "Loaded $(ls data/summaries/ | wc -l) prior report(s) from master"
      - name: Run monthly roll-up
        env:
          MINIMAX_API_KEY: ${{ secrets.MINIMAX_API_KEY }}
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: uv run horizon monthly
      - name: Deploy monthly to Pages
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git checkout -f master
          git pull origin master || true
          git checkout -f main -- docs/_config.yml docs/_includes docs/assets .github/scripts 2>/dev/null || true
          mkdir -p docs/_posts
          git checkout -f master -- docs/_posts 2>/dev/null || true
          cp data/summaries/*.md docs/_posts/ || true
          git add -f docs/
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "📆 Monthly Summary"
            git push origin master
          fi

  yearly-summary:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - uses: astral-sh/setup-uv@v6
      - run: uv sync
      - run: cp data/config.github.json data/config.json
      - name: Fetch prior reports from master → data/summaries/
        run: |
          # See weekly-summary job above for the full rationale. Chain-mode
          # rollups read from data/summaries/ which is .gitignored; we
          # materialise master docs/_posts/ into it on every rollup job.
          git fetch origin master --depth=1
          mkdir -p data/summaries
          # NUL-delimited listing: plain ls-tree quotes non-ASCII paths
          # (core.quotePath) and word-splitting breaks on spaces.
          git ls-tree -r --name-only -z origin/master docs/_posts/ | while IFS= read -r -d '' f; do
            base=$(basename "$f")
            git show "origin/master:$f" > "data/summaries/$base"
          done
          echo "Loaded $(ls data/summaries/ | wc -l) prior report(s) from master"
      - name: Run yearly roll-up
        env:
          MINIMAX_API_KEY: ${{ secrets.MINIMAX_API_KEY }}
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: uv run horizon yearly
      - name: Deploy yearly to Pages
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git checkout -f master
          git pull origin master || true
          git checkout -f main -- docs/_config.yml docs/_includes docs/assets .github/scripts 2>/dev/null || true
          mkdir -p docs/_posts
          git checkout -f master -- docs/_posts 2>/dev/null || true
          cp data/summaries/*.md docs/_posts/ || true
          git add -f docs/
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "🗓 Yearly Summary"
            git push origin master
          fi
